<Window x:Class="ApexClick.Views.PlaybackHudWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        SizeToContent="WidthAndHeight"
        Focusable="False">
    <Border Style="{StaticResource PanelBorderStyle}"
            BorderBrush="{StaticResource SteelGreyBrush}" BorderThickness="1"
            Padding="14,10" MinWidth="200">
        <StackPanel>
            <TextBlock Text="ApexClick" Style="{StaticResource UiTextStyle}" FontWeight="SemiBold" FontSize="14"
                       HorizontalAlignment="Center" Margin="0,0,0,4" />
            <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource SecondaryTextStyle}"
                       TextWrapping="Wrap" MaxWidth="220" HorizontalAlignment="Center" Margin="0,0,0,10" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="{Binding IsPlaybackPaused, Converter={StaticResource PauseResumeButtonLabelConverter}}"
                        Command="{Binding PauseResumePlaybackCommand}"
                        Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,6,0" />
                <Button Content="■ Стоп" Command="{Binding StopPlaybackCommand}"
                        Style="{StaticResource SecondaryButtonStyle}" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
